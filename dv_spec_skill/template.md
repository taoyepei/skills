# 验证方案模板

> **说明**：斜体 *文本* 表示需要填写内容的占位符

## 1 简介

### 1.1 目的

本文档用于各验证负责人在环境搭建前对验证策略进行制定，涵盖DUT的数据和控制流分析、验证重难点分析及策略、环境实现策略、用例/checker/coverage策略、验证入口和出口标准、主要的测试流程等等，方案的制订目的是做好验证规划，定好验证的策略，尽可能地避免验证工作返工，保证高质高效完成验证工作。

### 1.2 范围

团队内查阅。

### 1.3 参考文档

*列出参考文档。*

## 2 DUT设计介绍

### 2.1 设计架构图

*设计简介/特点/架构/使用场景等。参考LRS文档中的架构描述。*

### 2.2 模块主要特性

*模块主要特性介绍。*

### 2.3 控制和数据流

*DUT控制和数据流简述。*

## 3 验证策略

### 3.1 验证重点和难点

*描述验证重点和难点，以及将采取的验证策略*

### 3.2 验证平台和方法学

使用VCS仿真器，基于UVM验证方法学进行仿真验证。

### 3.3 自动化验证策略

*描述自动化验证策略，采用什么自动化措施提升验证效率。*

### 3.4 验证阶段划分

*对于UT/BT simulation，验证可以分为两个阶段：
*1. RTL simulation
*2. RTL xprop simulation：需要阐述xprop的配置*

## 4 验证平台架构

### 4.1 验证环境示意图

*使用 architecture-diagram skill 生成 HTML+SVG 框图，通过 selenium+Chrome 截图为 PNG，插入到文档中。框图需描述环境结构和工作机制、需要的组件及组件配置、环境在集成环境中挂载思考等。详细规则参见 diagram_rules.md 和 diagram_screenshot.md。*

### 4.2 验证组件实现方案

#### 4.2.1 Agent实现方案

*描述input/output Agent的实现思路，如果是第三方VIP，请给出集成方案。如果是自研Agent，给出interface/transaction/driver/monitor实现方案。*

#### 4.2.2 Reference model实现方案

*描述参考模型的实现思路，包括实现语言(SV/SystemC/…)、对外API、架构图/流程图*

#### 4.2.3 Checker实现方案

*描述checker比对的方案，需要注意和RTM中'Checker list'的区别：后者是列出有哪些checker，不会描述checker如何实现。*

### 4.3 覆盖率方案

*描述验证Coverage的实现方案，主要是介绍规划的Coverage以及实现方式和思路，包括但不限于：
*1. 分bin的方案
*2. cross coverage的方案
*3. 采样（sample）的方案*

### 4.4 环境重用方案

*描述环境或者组件重用的方案
*重用什么平台的什么组件
*自己的平台如何更好的被更高层次的验证平台重用*

## 5 Formal验证方案

*可选章节：对于采用了Formal验证的验证项，在本章中描述Formal验证的方案*

## 6 Testcase验证用例方案

### 6.1 Testcase执行流程

*描述用例测试主要流程，建议画出流程图，尤其是非纯UVM的验证环境。*

### 6.2 C-SV Cosim方案

*可选章节：对于采用C和SystemVerilog联合仿真的testcase，请给出C和SV的配合方式*

## 7 Risks风险和应对措施

*思考和描述验证"零"缺陷交付的困难与风险，如何应对，需要什么资源或者帮助。*
